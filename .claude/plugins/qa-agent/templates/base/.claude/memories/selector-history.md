# Selector history — {{APP_NAME}}

Every selector the healer replaces, with the reason. Append-only; never rewrite
history. A replacement must never downgrade selector priority (CLAUDE.md §9).

| Date | Module | Key | Old → New | Why |
|---|---|---|---|---|
| _(empty)_ | | | | |
