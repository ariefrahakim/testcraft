# Component map — {{APP_NAME}}

Shared UI components and the selectors that address them, so two modules touching
the same component reuse one locator instead of minting a second.

| Component | Where it appears | Selector | Owning locator file |
|---|---|---|---|
| _(empty — filled as modules are generated)_ | | | |
