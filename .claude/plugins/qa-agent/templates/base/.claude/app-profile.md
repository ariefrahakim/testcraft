# App Profile — {{APP_NAME}} ({{REGION}})

**Per-repo, NEVER upgraded.** `CLAUDE.md` is the shared, app-agnostic contract
shipped by the `qa-agent` plugin; this file holds everything specific to *this*
app-under-test. Every repo the plugin scaffolds keeps its own copy with its own
values. Whenever `CLAUDE.md` says "the active app" or "the configured Qase
project / FE path / Jira parent", it means the values declared here (and the
matching `.env`).

Generated by `/qa-agent:setup` on {{INIT_DATE}}.

| Variable | Value (this repo) | Backing env var |
|---|---|---|
| App under test | **{{APP_NAME}}** | `HOST`, `SSO_LOGIN_URL` |
| Region | {{REGION}} | — |
| Tool stack | **{{STACK}}** | — |
| Development style | **{{STYLE}}** | — |
<!-- #region qa-agent:jira -->
| Qase project | **{{QASE_PROJECT}}** | `QASE_PROJECT` |
<!-- #endregion qa-agent:jira -->
<!-- #region qa-agent:azure -->
| Azure DevOps org | `{{AZURE_ORG_URL}}` | `AZURE_ORG_URL` |
| Azure Boards project (tracker only — TMS is Qase) | **{{QASE_PROJECT}}** | `AZURE_PROJECT` |
<!-- #endregion qa-agent:azure -->
| FE source scan root | `{{FE_SCAN_ROOT}}/**` | `FE_REPO_PATH` |
| FE checkout root | `{{FE_DIR}}/` | `FE_REPO_ROOT` |
| FE repo | `{{FE_REPO_URL}}` (branch `{{FE_BRANCH}}`) | `FE_REPO_URL`, `FE_BRANCH` |
<!-- #region qa-agent:jira -->
| Jira tenant | `{{JIRA_BASE_URL}}` | `BASE_URL_ATLASSIAN` |
| Jira default parent (defects) | `{{JIRA_PROJECT_KEY}}-{{JIRA_PARENT_ISSUE_ID}}` | `JIRA_PROJECT_KEY` + `JIRA_PARENT_ISSUE_ID` |
<!-- #endregion qa-agent:jira -->
<!-- #region qa-agent:azure -->
| Default defect parent (work item) | `{{JIRA_PARENT_ISSUE_ID}}` | `AZURE_PARENT_WORK_ITEM_ID` |
<!-- #endregion qa-agent:azure -->
| Ticket prefixes | **{{JIRA_DELIVERY_PROJECT}}** — `[FE-…]`/`[BE-…]`/`[DB-…]`/`[TM-…]`/`[TA-…]`/`[QA]` | — |

## Tool stack — {{STACK}}

{{STACK_NOTES}}

Switching stacks is not an in-place operation: the provider modules, the
reporter and the CI file all differ. Scaffold a new repo with the other stack
and move `pages/`, `locators/`, `tests/`, `features/`, `steps/` and `fixtures/`
across — they are stack-agnostic by design.

## Development style — {{STYLE}}

This repo was scaffolded for **{{STYLE}}** (CLAUDE.md §5a). Concretely:

{{STYLE_NOTES}}

To add the other style later, run `/qa-agent:setup --add-style <tdd|bdd>` — it
adds only the missing layer (folders, fixtures, config project, npm scripts)
and never touches generated assets.

## App-specific notes that override / specialise the base contract

> Fill these in as the team learns the app. This section is the right home for
> anything that would otherwise get hardcoded into the shared contract.

- **Selector source-of-truth (CLAUDE.md §3 step 3):** scan `{{FE_SCAN_ROOT}}/**`
  exclusively. If the FE is a monorepo, never mine testids from a sibling app
  or the repo root.
- **Testid convention:** _TODO — record the FE's documented pattern here (e.g.
  `[page]-[section]-[action]-btn`) once confirmed._
- **Login selectors (CLAUDE.md §4):** the login screen commonly predates the
  testid standard — the seed `locators/login/login.locator.ts` falls back to
  form `name` attributes. Promote to testids as soon as the FE adds them.
- **Post-login landing route:** `{{POST_LOGIN_URL_PATTERN}}` (override via
  `POST_LOGIN_URL_PATTERN` in `.env`).
- **Tier / role users:** _TODO — list any additional accounts the plans need
  and the env vars that carry them._
- **Curated test data** that is not parsed from a Qase plan description lives in
  the `App-specific config` section of `shared/env.ts` (env-overridable).

## Session / concurrency constraints

- _TODO — confirm whether the app allows more than one active session per user._
  The scaffold pins `workers: 1` (CLAUDE.md §13) because single-SSO apps evict
  each other's sessions under parallel workers. Raise it only with a pool of
  distinct test accounts.
